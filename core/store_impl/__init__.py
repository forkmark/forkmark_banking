"""Forkmark data-layer implementation, split into per-domain repository mixins.

The public ``Database`` class and all module-level helpers are re-exported from
``core.store`` (the stable import path). Import from there, not from here."""
